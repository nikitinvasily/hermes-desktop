/**
 * Remote-parity transition (issue #51): SSH connections are superseded by the
 * direct dashboard connection. Creation UI is hidden while existing SSH
 * connections keep working — flip to true to re-expose the SSH options.
 */
export const SSH_UI_ENABLED = false;
